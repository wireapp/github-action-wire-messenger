/// <reference types="node" />
import { EncodedData } from './EncodedData';
export declare class Encoder {
    static toBase64(data: string | number | number[] | ArrayBuffer | Buffer | Uint8Array): EncodedData;
    private static fromByteArray;
}

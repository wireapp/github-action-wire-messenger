#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@wireapp/protocol-messaging/node_modules/protobufjs/bin/pbts" "$@"
  ret=$?
else 
  node  "$basedir/../@wireapp/protocol-messaging/node_modules/protobufjs/bin/pbts" "$@"
  ret=$?
fi
exit $ret

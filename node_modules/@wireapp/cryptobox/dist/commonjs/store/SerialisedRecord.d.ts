/** Record that is written into the store */
export declare class SerialisedRecord {
    created: number;
    id: string;
    serialised: string;
    version: string;
    constructor(serialised: string, id: string);
}

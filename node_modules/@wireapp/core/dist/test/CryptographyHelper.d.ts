import * as Proteus from '@wireapp/proteus';
import { PreKey } from '@wireapp/proteus/dist/keys';
import { CryptographyService } from '../cryptography';
export declare function createEncodedCipherText(receiver: Proteus.keys.IdentityKeyPair, preKey: PreKey, text: string): Promise<string>;
export declare function getPlainText(cryptographyService: CryptographyService, encodedPreKeyMessage: string, sessionId?: string): Promise<string | void>;

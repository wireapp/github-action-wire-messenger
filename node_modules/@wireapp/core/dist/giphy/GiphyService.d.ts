import { APIClient } from '@wireapp/api-client';
import { GiphySearch, GiphySearchResult, GiphyResult } from '@wireapp/api-client/dist/giphy/';
export declare class GiphyService {
    private readonly apiClient;
    constructor(apiClient: APIClient);
    getRandomGif(tag?: string): Promise<GiphyResult>;
    searchGif(options: GiphySearch): Promise<GiphySearchResult>;
}

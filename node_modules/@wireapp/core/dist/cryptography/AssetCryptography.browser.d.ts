/// <reference types="node" />
import { EncryptedAsset } from '../cryptography/';
export declare const decryptAsset: ({ cipherText, keyBytes, sha256: referenceSha256, }: EncryptedAsset) => Promise<Buffer>;
export declare const encryptAsset: (plaintext: ArrayBuffer) => Promise<EncryptedAsset>;

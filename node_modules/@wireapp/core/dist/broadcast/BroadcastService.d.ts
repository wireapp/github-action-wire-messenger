import { APIClient } from '@wireapp/api-client';
import { GenericMessage } from '@wireapp/protocol-messaging';
import { ConversationService } from '../conversation/';
import { CryptographyService } from '../cryptography/';
export declare class BroadcastService {
    private readonly apiClient;
    private readonly conversationService;
    private readonly cryptographyService;
    constructor(apiClient: APIClient, conversationService: ConversationService, cryptographyService: CryptographyService);
    private getPreKeyBundle;
    broadcastGenericMessage(teamId: string, genericMessage: GenericMessage): Promise<void>;
    private sendOTRBroadcastMessage;
}

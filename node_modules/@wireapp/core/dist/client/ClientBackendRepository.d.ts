import { APIClient } from '@wireapp/api-client';
import { NewClient, RegisteredClient } from '@wireapp/api-client/dist/client/';
export declare class ClientBackendRepository {
    private readonly apiClient;
    constructor(apiClient: APIClient);
    getClients(): Promise<RegisteredClient[]>;
    postClient(client: NewClient): Promise<RegisteredClient>;
}

import { CRUDEngine } from '../engine';
export declare const readSpec: {
    'returns a database record.': (engine: CRUDEngine) => Promise<void>;
    'throws an error if a record cannot be found.': (engine: CRUDEngine) => Promise<void>;
};

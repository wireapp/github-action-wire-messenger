import { CRUDEngine } from '../engine';
export declare const purgeSpec: {
    'database can be reinitialized after purge': (engine: CRUDEngine, initEngine: (shouldCreateNewEngine?: boolean | undefined) => Promise<CRUDEngine>) => Promise<void>;
    'deletes the database and all of its records.': (engine: CRUDEngine, initEngine: (shouldCreateNewEngine?: boolean | undefined) => Promise<CRUDEngine>) => Promise<void>;
};

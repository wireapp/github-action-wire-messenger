import { CRUDEngine } from '../engine';
export declare const createSpec: {
    "doesn't save empty values.": (engine: CRUDEngine) => Promise<void>;
    "doesn't save null values.": (engine: CRUDEngine) => Promise<void>;
    'saves objects.': (engine: CRUDEngine) => Promise<void>;
    'saves strings and objects in the same table.': (engine: CRUDEngine) => Promise<void>;
    'saves strings.': (engine: CRUDEngine) => Promise<void>;
    'throws an error when attempting to overwrite a record.': (engine: CRUDEngine) => Promise<void>;
};

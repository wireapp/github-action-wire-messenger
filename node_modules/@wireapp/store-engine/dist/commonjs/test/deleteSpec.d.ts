import { CRUDEngine } from '../engine';
export declare const deleteSpec: {
    'deletes a record.': (engine: CRUDEngine) => Promise<void>;
    'returns the primary key of a deleted record.': (engine: CRUDEngine) => Promise<void>;
};

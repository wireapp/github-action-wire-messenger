import { CRUDEngine } from '../engine';
export declare const updateSpec: {
    'fails if the record does not exist.': (engine: CRUDEngine) => Promise<void>;
    'updates an existing database record.': (engine: CRUDEngine) => Promise<void>;
};

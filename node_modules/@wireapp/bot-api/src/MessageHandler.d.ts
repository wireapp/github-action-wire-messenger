import type { Conversation, UserClients } from '@wireapp/api-client/src/conversation';
import { CONVERSATION_TYPING } from '@wireapp/api-client/src/conversation/data';
import type { ConversationEvent, TeamEvent, UserEvent } from '@wireapp/api-client/src/event';
import type { User } from '@wireapp/api-client/src/user/';
import type { Account } from '@wireapp/core';
import type { PayloadBundle, ReactionType } from '@wireapp/core/src/main/conversation/';
import type { CallingContent, FileContent, FileMetaDataContent, ImageContent, LinkPreviewContent, LocationContent, MentionContent } from '@wireapp/core/src/main/conversation/content/';
import type { QuotableMessage } from '@wireapp/core/src/main/conversation/message/OtrMessage';
export declare abstract class MessageHandler {
    account: Account | undefined;
    abstract handleEvent(payload: PayloadBundle | ConversationEvent | UserEvent | TeamEvent): void;
    addUser(conversationId: string, userId: string): Promise<void>;
    clearConversation(conversationId: string): Promise<void>;
    getConversation(conversationId: string): Promise<Conversation>;
    getConversations(conversationIds?: string[]): Promise<Conversation[]>;
    getUser(userId: string): Promise<User>;
    getUsers(userIds: string[]): Promise<User[]>;
    removeUser(conversationId: string, userId: string): Promise<void>;
    setAdminRole(conversationId: string, userId: string): Promise<void>;
    setMemberRole(conversationId: string, userId: string): Promise<void>;
    sendButtonActionConfirmation(conversationId: string, userId: string, referenceMessageId: string, buttonId: string): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendCall(conversationId: string, content: CallingContent, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendPoll(conversationId: string, text: string, buttons: string[], userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendConfirmation(conversationId: string, firstMessageId: string, userIds?: string[] | UserClients): Promise<void>;
    sendConnectionRequest(userId: string): Promise<void>;
    sendConnectionResponse(userId: string, accept: boolean): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendEditedText(conversationId: string, originalMessageId: string, newMessageText: string, newMentions?: MentionContent[], newLinkPreview?: LinkPreviewContent, userIds?: string[] | UserClients): Promise<void>;
    sendFileByPath(conversationId: string, filePath: string, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendFile(conversationId: string, file: FileContent, metadata: FileMetaDataContent, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendImage(conversationId: string, image: ImageContent, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendLocation(conversationId: string, location: LocationContent, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendPing(conversationId: string, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendReaction(conversationId: string, originalMessageId: string, type: ReactionType, userIds?: string[] | UserClients): Promise<void>;
    sendQuote(conversationId: string, quotedMessage: QuotableMessage, text: string, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendReply(conversationId: string, quotedMessage: QuotableMessage, text: string, userIds?: string[] | UserClients): Promise<void>;
    /**
     * @param userIds Only send message to specified user IDs or to certain clients of specified user IDs
     */
    sendText(conversationId: string, text: string, mentions?: MentionContent[], linkPreview?: LinkPreviewContent, userIds?: string[] | UserClients): Promise<void>;
    sendTyping(conversationId: string, status: CONVERSATION_TYPING): Promise<void>;
}

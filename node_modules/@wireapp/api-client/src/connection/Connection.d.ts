import type { ConnectionStatus } from '../connection/';
export interface Connection {
    conversation: string;
    from: string;
    last_update: string;
    message?: string;
    status: ConnectionStatus;
    to: string;
}

import { GiphySearch, GiphyResult, GiphySearchResult } from '../giphy/';
import { HttpClient } from '../http/';
export declare class GiphyAPI {
    private readonly client;
    constructor(client: HttpClient);
    static readonly URL: {
        GIPHY: string;
        PROXY: string;
        RANDOM: string;
        SEARCH: string;
    };
    /**
     * Get a Giphy image by its ID.
     * @param ids one or multiple image ID(s)
     */
    getGiphyById(ids: string | string[]): Promise<GiphyResult>;
    /**
     * Get a random GIF from Giphy.
     * @param tag GIF tag to limit randomness
     */
    getGiphyRandom(tag?: string): Promise<GiphyResult>;
    /**
     * Get GIF search results from Giphy.
     * @param options Search options
     */
    getGiphySearch(options: GiphySearch): Promise<GiphySearchResult>;
}

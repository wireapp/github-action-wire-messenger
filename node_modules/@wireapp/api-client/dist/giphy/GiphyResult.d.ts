import { GiphyImage } from './GiphyImage';
export interface GiphyResult {
    data: GiphyImage;
    meta: {
        status: number;
        msg: string;
        response_id: string;
    };
}
export declare type GiphySearchResult = GiphyResult & {
    data: GiphyImage[];
    pagination: {
        count: number;
        offset: number;
        total_count: number;
    };
};

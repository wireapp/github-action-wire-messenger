import { ClientType } from '../client/';
export interface LoginData {
    code?: string;
    email?: string;
    handle?: string;
    password?: number | string;
    clientType: ClientType;
    phone?: string;
}

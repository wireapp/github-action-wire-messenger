import { HttpClient } from '../http/';
import { AssetRetentionPolicy } from './AssetRetentionPolicy';
import { AssetUploadData } from './AssetUploadData';
export declare class AssetAPI {
    private readonly client;
    private static readonly ASSET_URL;
    constructor(client: HttpClient);
    getAsset(assetId: string, token?: string | null): Promise<ArrayBuffer>;
    postAsset(asset: Uint8Array, options?: {
        public: boolean;
        retention: AssetRetentionPolicy;
    }): Promise<AssetUploadData>;
}

import { HttpClient } from '../../http';
import { IdentityProviderStatus } from './IdentityProviderStatus';
export declare class FeatureAPI {
    private readonly client;
    constructor(client: HttpClient);
    static readonly URL: {
        FEATURES: string;
        SSO: string;
        TEAMS: string;
    };
    getIdentityProviderStatus(teamId: string): Promise<IdentityProviderStatus>;
}

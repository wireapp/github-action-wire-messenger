import { HttpClient } from '../../http';
import { PropertyData } from './PropertyData';
export declare class PropertyAPI {
    private readonly client;
    constructor(client: HttpClient);
    static readonly URL: {
        PROPERTIES: string;
        SIGNATURES: string;
    };
    getPropertyData(teamId: string): Promise<PropertyData>;
}

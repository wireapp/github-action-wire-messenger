import * as CBOR from '@wireapp/cbor';
import { MacKey } from '../derived/MacKey';
import { MessageKeys } from './MessageKeys';
export declare class ChainKey {
    readonly idx: number;
    readonly key: MacKey;
    private static readonly propertiesLength;
    constructor(key: MacKey, index?: number);
    static from_mac_key(key: MacKey, counter: number): ChainKey;
    next(): ChainKey;
    message_keys(): MessageKeys;
    encode(encoder: CBOR.Encoder): CBOR.Encoder;
    static decode(decoder: CBOR.Decoder): ChainKey;
}

import * as CBOR from '@wireapp/cbor';
import { IdentityKey } from './IdentityKey';
import { SecretKey } from './SecretKey';
export declare class IdentityKeyPair {
    readonly public_key: IdentityKey;
    readonly secret_key: SecretKey;
    readonly version: number;
    private static readonly propertiesLength;
    constructor(public_key: IdentityKey, secret_key: SecretKey, version?: number);
    static new(): Promise<IdentityKeyPair>;
    serialise(): ArrayBuffer;
    static deserialise(buf: ArrayBuffer): IdentityKeyPair;
    encode(encoder: CBOR.Encoder): CBOR.Encoder;
    static decode(decoder: CBOR.Decoder): IdentityKeyPair;
}

import * as CBOR from '@wireapp/cbor';
import { PublicKey } from '../keys/PublicKey';
import { Message } from './Message';
import { SessionTag } from './SessionTag';
export declare class CipherMessage extends Message {
    readonly cipher_text: Uint8Array;
    readonly counter: number;
    readonly prev_counter: number;
    readonly ratchet_key: PublicKey;
    readonly session_tag: SessionTag;
    private static readonly propertiesLength;
    constructor(sessionTag: SessionTag, counter: number, prevCounter: number, ratchetKey: PublicKey, cipherText: Uint8Array);
    encode(encoder: CBOR.Encoder): CBOR.Encoder;
    static decode(decoder: CBOR.Decoder): CipherMessage;
}
